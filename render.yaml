services:
  - type: web
    name: social-tracker-proxy
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node proxy.js
    envVars:
      - key: NODE_VERSION
        value: 20
